# T-2.1  Split Slice 1 into 1a and 1b

file: docs/ROADMAP.md
spec: —
blocked_by: none
done_when: ROADMAP.md describes two provable slices and nothing else changed

## Required change
"Slice 1 — the tracer bullet" currently contains the whole of v1: four entry modes, four
presets x three placements, Tumbler, limited library, reconciliation, drag-select batch
restore, and two tiers of statistics. A tracer bullet is one path.

Rewrite it as:
  Slice 1a — one preset (Swipe), Recent only, queue → review → commit → result,
             proven on a real device or Simulator. (Currently: the smallest thing that
             can go red and tell you something.)
  Slice 1b — the four presets and placements, Start Here, Tumbler, limited-library
             affordance, statistics.

Move every existing bullet into 1a or 1b. Do not delete a bullet. Keep the existing
"Proven when" structure and write one for each.
Slices 2, 3, 4 and "Explicitly out of scope" stay as they are.

## Do not
touch SPEC.md, VISION.md, or any code.

## Report
`git diff docs/ROADMAP.md`, and which bullets you assigned to 1a.
