# T-5.3  Prune AGENTS.md and link the test doc

file: AGENTS.md, docs/TESTING.md
spec: —
blocked_by: T-0.1, T-0.2, T-3.1, T-4.1
status: not-done
done_when: AGENTS.md is shorter than before, every entry points at an authoritative file, and nothing the code already shows is restated

## Required change
`AGENTS.md` tells future agents to "prefer rewriting or pruning existing entries over
appending new ones". Do that now.

  - Add a "Build and test" pointer to `docs/TESTING.md` (from T-0.1).
  - Update "Architecture" if T-4.1 changed the AppModel description.
  - Add one line: regenerating the project rewrites every UUID in `Swiper.xcodeproj`, so
    the first merged PR will show a large diff. (Note it; do not try to fix it.)
  - Delete any entry the code or another doc already shows. Aim to remove at least as many
    lines as you add.

## Do not
restate file contents, add a changelog, or exceed the existing file's length by more
than two lines.

## Report
`git diff --stat AGENTS.md` with the line count going down, and what you removed.
