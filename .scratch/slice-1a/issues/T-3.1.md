# T-3.1  Ponytail pass — delete the unused surface

file: the 12 files named below
spec: —
blocked_by: T-1.1, T-1.2, T-1.4   (all three edit files this ticket touches)
status: not-done
done_when: the suite is still green, `git diff --stat` shows deletions only, and no line added

## Contract
This ticket is DELETION ONLY. If any item turns out not to be safely deletable, skip it
and name it in your report — do not refactor around it. Delete the tests that only
existed to cover a deleted API.

## The list (in this order)
 1. SessionEngine.remainingCount — no callers                              [SwiperKit/SessionEngine.swift]
 2. SessionStatistics.hasCurrentSessionActivity — no callers                [SwiperKit/SessionStatistics.swift]
 3. ByteFormatter.approximateString(fromBytes:) — SessionResultView inlines the word [SwiperKit/ByteFormatter.swift]
 4. LibraryOrder.neighborIndex(from:direction:) and asset(at:) — no production caller [SwiperKit/LibraryOrder.swift]
 5. ReconciledSession.externallyRemovedIDs — KEEP the filtering, drop the reported field [SwiperKit/AssetReconciler.swift]
 6. SessionAction.undo + SessionAction.title + its allCases — undo is reached via engine.undo() [SwiperKit/Models.swift]
 7. UndoStack.canUndo / count / removeAll — make `capacity` a private constant [SwiperKit/UndoStack.swift]
 8. MediaKind: CaseIterable — allCases used nowhere [SwiperKit/Models.swift]
 9. SessionEffect.noOp — return [] instead; update AppModel.perform [SwiperKit/Models.swift, Swiper/ViewModels/AppModel.swift]
10. AdaptiveButtonStyle — choose the style at the one call site [Swiper/Views/Components.swift, Swiper/Views/EntryView.swift]
11. ByteFormatter PB unit + the second rounding branch — cap at TB [SwiperKit/ByteFormatter.swift]
12. Scripts/generate_app_icon.swift — keep the committed PNG [Scripts/generate_app_icon.swift]

## Do not
touch anything not on this list. Do not change behaviour. Do not "tidy while you're there".

## Report
The green suite output, `git diff --stat`, and any item you skipped with the reason.
