# T-2.2  Re-key the tickets to 1a / 1b

file: .scratch/slice-1a/issues/
spec: —
blocked_by: T-2.1
status: not-done
done_when: every ticket declares slice 1a or 1b, and no ticket is unassigned

## Required change
For each ticket in `.scratch/slice-1a/issues/`, add `slice: 1a` or `slice: 1b` using the
new ROADMAP.md. Create the missing 1b tickets (one file each) for the work in 1b that no
existing ticket covers. Keep the same ticket format and the same directory — do not
create a second queue.

The four existing Phase 1 tickets are all 1a: they are correctness work on the core path.

## Do not
change any 1a ticket's `done_when`, and do not start work on any 1b ticket.

## Report
The full ticket list with slice + status, `git diff --stat`.
