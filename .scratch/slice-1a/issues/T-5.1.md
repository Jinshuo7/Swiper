# T-5.1  Sync SPEC.md to the code

file: docs/SPEC.md
spec: —
blocked_by: T-1.2, T-1.3, T-4.1
status: not-done
done_when: every behaviour changed in Phase 1 and 4 has a matching SPEC clause

## Required change
Read the diffs from T-1.2, T-1.3 and T-4.1 — not the repo. Then update SPEC.md:

  - §5 (resume and persistence): state what happens to an unreadable or future-version
    session file, now that T-1.2 made it distinguishable from "no session".
  - §3 (viewer): state what a decision does while a favorite write is in flight,
    per the policy T-1.3 chose.
  - §7 (statistics): define whether a resumed session counts as the same session for
    `currentSessionDeletedCount`, which currently resets only in `startSession()`.
  - §5: state explicitly whether a user may finish a session with a non-empty deletion
    queue. `ViewerView.finishedOverlay` currently offers only "Review N for deletion",
    so the answer is probably "no" — write the clause either way.

Keep the numbered-clause style. Every clause you touch must be checkable by a test.

## Do not
change VISION.md, ROADMAP.md, or any code.

## Report
`git diff docs/SPEC.md`, with the clause numbers you added or changed.
