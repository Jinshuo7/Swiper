# T-0.2  Record the iOS typecheck status

file: Scripts/typecheck-ios.sh, docs/TESTING.md
spec: —
blocked_by: T-0.1
status: done
done_when: docs/TESTING.md states the typecheck command and its real result, or names the exact blocker

## Required change
Run `Scripts/typecheck-ios.sh`. Generalise its hardcoded paths the same way T-0.1 did
(`$DEVELOPER_DIR`, `xcode-select -p`, host arch, installed SDK).

If it goes green, record the command and result in `docs/TESTING.md`.
If it cannot run (Xcode licence not accepted, iOS simulator runtime not installed), do
NOT work around it. Write one honest line in `docs/TESTING.md`: the exact error text, and
the one command the user must run to fix it.

## Do not
attempt to install the iOS platform, accept a licence, or change machine-wide state.

## Report
The command, the real output or exact error text, `git diff --stat`.
