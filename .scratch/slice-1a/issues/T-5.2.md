# T-5.2  ADR: versioned session persistence

file: docs/adr/0004-schema-versioned-session-persistence.md
spec: —
blocked_by: T-1.2
status: not-done
done_when: the ADR records the decision T-1.2 actually made

## Required change
Write `docs/adr/0004-schema-versioned-session-persistence.md` in the style of 0001–0003:
a short title, the situation, the decision, the alternative that was rejected and why,
and the consequences. Match the existing files' length and tone — they are ~950 bytes
each, terse and specific.

Record: why a persisted session must be versioned at all; what the chosen behaviour is
for a lower and a higher `schemaVersion`; and what the rejected alternative was
(silently treating a decode failure as "no session").

## Do not
write ADR-0005 or edit ADR-0001–0003. Do not add a "further reading" section — the
existing ADRs do not have one.

## Report
The ADR file's content, `git diff --stat`.
