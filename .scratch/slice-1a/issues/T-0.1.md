# T-0.1  Get a green run of the pure-logic suite

file: Scripts/run-kit-tests.sh
spec: —
blocked_by: none
done_when: `Scripts/run-kit-tests.sh` exits 0 and its output shows N passing tests

## Required change
The script hardcodes `TARGET=arm64-apple-macosx26.0`, `MACSDK` under
`/Applications/Xcode.app/...`, and a fixed `XCODE_DIR`. Generalise without changing its
purpose: honour `$DEVELOPER_DIR` if set, otherwise take the developer root from
`xcode-select -p`; detect the host architecture (`uname -m`) and the installed macOS SDK
version rather than hardcoding `26.0`. Keep the "no xcodebuild needed" design.

## Done
Create `docs/TESTING.md` recording: the exact command, the pass count, and anything a
future agent must know to re-run it. Link it from `AGENTS.md` under "Build and test".

## Do not
touch anything outside `Scripts/run-kit-tests.sh` and `docs/TESTING.md`.

## Report
The exact command you ran, its real output, `git diff --stat`.
