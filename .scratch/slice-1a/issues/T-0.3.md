# T-0.3  Verify the ticket queue against the tree

file: .scratch/slice-1a/issues/
spec: —
blocked_by: T-0.2
status: done
done_when: every ticket file has a `status:` line judged against the current tree

## Required change
The ticket queue already exists at `.scratch/slice-1a/issues/` (it was created alongside this
ticket). Do not move or re-create it.

For each ticket file, add a `status:` line — `done`, `not-done` or `n/a` — judged against
the CURRENT tree, not against the roadmap. Concretely: does the code the ticket names still
contain what the ticket says it contains?

Also correct any ticket whose `file:` or `blocked_by:` line is wrong.

Do not change any ticket's `done_when`. If a ticket looks wrong, fix its `file:` or
`blocked_by:` line and say why in your report.

## Do not
change source code, specs or docs. This ticket touches only `.scratch/slice-1a/issues/`.

## Report
`ls .scratch/slice-1a/issues/`, the status you assigned each ticket, `git diff --stat`.
